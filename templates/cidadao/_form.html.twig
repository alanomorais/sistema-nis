{{ form_start(form) }}
    
    <div class="mb-4">
        {{ form_label(form.nome, 'Nome:', {'class': 'block text-gray-700 font-bold mb-2'}) }}
        {{ form_widget(form.nome, {'class': 'form-input w-full'}) }}
    </div>    
    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
